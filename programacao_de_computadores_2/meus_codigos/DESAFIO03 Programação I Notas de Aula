// Fa¸ca um programa que leia 3 valores a, b e c, coeficientes de uma equa¸c˜ao de segundo grau, e calcule e escreva as raizes da equa¸c˜ao. As ra´?zes de uma equa¸c˜ao podem ser calculadas pela f´ormula de Baskhara:
// Autor : Eduardo Bauer
// Data : 02/03/2024
#include<stdio.h>
#include<math.h>
void main()
{
         float a, b, c, baskhara, x1, x2;
         printf("Digite o valor de a da baskhara: ");
         scanf("%f", &a);
         printf("Digite o valor de b da baskhara: ");
         scanf("%f", &b);
         printf("Digite o valor de c da baskhara: ");
         scanf("%f", &c);
         baskhara = sqrt(pow(b,2) - 4 * a* c);
         x1 = (-b - baskhara) / (2 * a);
         x2 = (-b + baskhara) / (2 * a);
         printf("Os valores sao: x1 = %.2f e x2 = %.2f", x1, x2);
}
